{% extends 'base.html.twig' %}

{% block title %}Edit candidature n°{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Edition de la candidature n°{{ offre.id }}</h1>

        {{ include('offre/_form.html.twig', {'button_label': 'Mise à jour'}) }}

        <a href="{{ path('app_home') }}" class="btn btn-primary">Retour</a>

        {{ include('offre/_delete_form.html.twig') }}
    </div>
{% endblock %}
